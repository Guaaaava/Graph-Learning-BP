# 项目评估报告：基于 GNN 的协同定位图拓扑优化

> 日期: 2026-06-15

---

## 一、项目概述

这是一个**图神经网络 (GNN) 驱动的协同定位网络拓扑剪枝**研究项目。核心思路：

1. **场景**：30 个 agent + 4 个 anchor 在 100m×100m 区域内，通过测距链路形成通信图
2. **目标**：训练 GNN 学会选择性剪除冗余边，在保持定位精度的前提下大幅降低通信开销
3. **方法**：Graph Information Bottleneck (GIB) 损失 — 平衡 Fisher 信息保留与图结构压缩
4. **评估**：粒子化非参数 Belief Propagation (BP) 在三种拓扑 (Dense/BFS/GNN) 上的定位精度对比

### 技术架构

```
数据生成 → GNN 训练(GIB损失) → 拓扑剪枝 → 粒子BP定位 → 多指标评估
  │              │                │            │            │
  │    EdgePredictorGNN       STE 0/1掩码   非参数BP     RMSE/中断率
  │    5维节点+4维边          62%边减少      P=2000      /NEES/一致性
```

## 二、当前进展

| 模块 | 状态 | 说明 |
|------|------|------|
| 数据生成 | ✅ 完成 | challenge 模式，含 NLOS (15%)，2000/500/500 划分 |
| GNN 模型 | ✅ 完成 | 3层 MessagePassing + Gumbel-Sigmoid STE，~103K 参数 |
| GIB 损失 | ✅ 完成 | logdet(FIM⁻¹) + KL + degree + sparsity，四合一 |
| 训练 | ✅ 完成 | 200 epochs，AdamW + CosineAnnealing，温度退火 |
| 粒子 BP | ✅ 完成 | 从 NEBP 改写为 PyG，支持边权重剪枝，含 Roughening |
| 评估 | ⚠️ 部分 | 仅测 100/500 张图，P=2000（非最终 P=10000） |
| 代码整理 | ❌ 未做 | 大量改动未提交，NEBP/ 旧代码未删 |

### 当前结果 (100张图, P=2000, iter=10)

| 指标 | Dense | BFS | GNN |
|------|-------|-----|-----|
| 边数 | 391 | 36 | 147 |
| RMSE (m) | 1.49 | 5.76 | 2.44 |
| 中断率 (>3m) | 3.8% | 70.4% | 18.0% |
| 一致性 | 33.2% | 62.5% | 21.5% |
| NEES | 19.4 | 10.1 | 35.9 |

**边减少 62%，RMSE 退化 1.64×**。GNN 在 Dense 和 BFS 之间取得了折中，但精度损失显著。

---

## 三、问题分析

### 🔴 严重问题

#### 1. CRLB 指标计算错误

`evaluate.py:210` 将 `compute_gib_loss` 的 FIM loss 项 (`logdet(FIM⁻¹)`) 直接当作"CRLB"报告，**这是错误的**：

- `logdet(FIM⁻¹)` 是整个 FIM 的行列式对数，不是定位误差下界
- 实际 CRLB (米) 应为：`sqrt(mean(trace([FIM⁻¹]ᵢᵢ)))` — 每个 agent 2×2 对角块迹的均方根
- 这导致报告中出现 **负的 CRLB 值** (-108.2, -79.4)，毫无物理意义
- 报告中的 CRLB 数值完全不可比，Dense 的 -108 和 BFS 的 +34 跨度巨大但无法解读

**修复**：需要单独实现真正的 CRLB 计算函数，而不是复用 loss 函数。

#### 2. NEES 严重偏高 — 协方差估计崩溃

理想 NEES ≈ 2（2D 定位的卡方期望），实际 NEES 高达 10~36：

- **粒子 BP 的协方差估计极度低估了实际误差**（过度自信）
- 一致性仅 21~33%（应接近 95%），说明估计的 95% 置信椭圆远小于实际误差散布
- 可能原因：粒子数不足 (P=2000)、Roughening 噪声太小 (reg_scale=0.05)、重采样导致粒子贫化

**这直接影响了评估的可信度**：如果协方差估计是错的，RMSE 之外的指标（中断率、一致性）都可能被污染。

#### 3. GNN 剪枝质量不佳

62% 边减少的代价是：

- RMSE 退化 **64%**（1.49→2.44m）
- 中断率飙升 **4.7×**（3.8%→18.0%）
- 这个 trade-off 是否"值得"存疑——如果场景对精度敏感，18% 中断率不可接受

可能原因：

- GNN **没有学到几何上有意义的剪枝策略**，可能随机删边或删除了关键几何约束边
- 损失函数中 `SPARSITY_WEIGHT=150` 可能过强，过度惩罚边的存在
- FIM 项着眼于全局信息行列式，不直接对应单 agent 定位精度

### 🟡 中等问题

#### 4. 损失函数中的目标错配

GIB 损失优化的是 `logdet(FIM⁻¹)`（全局信息行列式），但评估用的是粒子 BP 的 RMSE。这两个目标不直接等价：

- `logdet` 趋于 0 意味着信息矩阵接近单位阵，但不保证每个 agent 的 CRLB 都小
- 可能存在"富人更富"效应：中心 agent 获得大量信息，边缘 agent 被牺牲

**建议**：考虑添加 per-agent CRLB 方差惩罚，或直接以粒子 BP 结果作为训练信号（RL/策略梯度）。

#### 5. 训练-评估的粒子数不匹配

- 训练中的 FIM 计算使用**真实位置**（oracle），理论上界
- 评估中粒子 BP 使用**含噪初值 + 测距**，实际操作
- 两者的 gap 没有量化分析

这是方法层面的固有限制（GIB 需要 oracle FIM），但需要在论文中明确讨论。

#### 6. 评估不完整

- 仅测了 100/500 张图，P=2000 而非最终 P=10000
- 没有统计显著性检验（方差/置信区间）
- 缺少消融实验：不同 λ、η、sparsity_weight 的影响
- 缺少与更多 baseline 的对比（如 MST 最小生成树、k-NN、随机剪枝）

### 🟢 次要问题

#### 7. 代码工程问题

- 所有改动未提交（git status 显示大量 modified + untracked）
- `NEBP/` 旧代码仍在但标注"待删除"
- `summary_0615.md` 中已识别的问题没有系统性跟踪（无 issue/todo）
- `compute_gib_loss` 职责过重：既算训练损失又当评估工具，参数通过设零来"关闭"不需要的项，不够清晰

#### 8. FIM 计算中的潜在数值问题

`loss.py:129` 的 `torch.linalg.eigvalsh(FIM)` 在 FIM 接近奇异时可能不稳定。虽然有 `prior_weight=0.5` 做对角加载，但这个值的选取缺乏理论依据。

---

## 四、优先级建议

| 优先级 | 任务 | 预期效果 |
|--------|------|----------|
| **P0** | 修复 CRLB 计算 → 实现真正的 per-agent CRLB (米) | 评估指标有意义 |
| **P0** | 排查 NEES 偏高 → 增大粒子数/reg_scale，验证协方差一致性 | 评估可信度 |
| **P1** | 超参数调优 → 降低 sparsity_weight，平衡 RMSE vs 边数 | GNN 性能提升 |
| **P1** | 完成完整评估 → 500 张图 + P=10000 + 统计检验 | 结果可发表 |
| **P2** | 添加 per-agent CRLB 约束 → 防止边缘 agent 被牺牲 | 方法改进 |
| **P2** | 更多 baseline 对比 (MST, k-NN, random) | 实验完整性 |
| **P3** | 代码整理 + 提交 + 删除 NEBP | 工程规范 |

---

## 五、总体评价

**方向正确，方法合理，但离可发表还有距离。** 项目已完成端到端 pipeline（数据→训练→评估），架构设计有清晰的理论依据（GIB + STE + 粒子BP）。当前最核心的问题是：

1. **评估指标有 bug**（CRLB 算错），需要立即修正
2. **协方差估计不可信**（NEES 爆表），需要深入排查粒子 BP
3. **GNN 剪枝精度损失偏大**（+64% RMSE），需要调参或改进方法

建议优先修 P0 问题，然后跑完整评估再看数据是否支撑当前方法。
